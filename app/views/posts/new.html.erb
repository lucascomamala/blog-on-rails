<div class="show-card container">
<%= bootstrap_form_with(model: @new_post, url: user_posts_path) do |form| %>
  <%= form.text_field :title, class: "form-control", required: true %>
  <%= form.text_area :content, rows: 15, class: "form-control", required: true %>
  <%= form.submit "Create Post", class: "btn btn-primary" %>
<% end %>
</div>
